<Window x:Class="Revit2022API351.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Revit2022API351"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="300">
    <StackPanel>
        <Label Content="Подсчет количества труб"
               Margin="5"/>
        <Button Content="Вычислить"
                Command="{Binding CalculateNumberОfPipes}"/>
        <Label Content="Подсчет объема всех стен"
               Margin="5"/>
        <Button Content="Вычислить"
               Command="{Binding CalculateVolumeОfWalls}"/>
        <Label Content="Подсчет количества дверей"
               Margin="5"/>
        <Button Content="Вычислить"
                Command="{Binding CalculateNumberОfDoors}"/>
    </StackPanel>
</Window>
